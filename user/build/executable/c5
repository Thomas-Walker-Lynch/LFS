#!/bin/bash

set -e

section="
binutils 
gcc
linux
glibc
"

IFS=$'\n'; for s in $section;do
  # echo s6_$s
  eval frame environment_package $s s5_$s
done

# libstdc++ is found in the gcc package:
frame environment_package gcc s5_libstdc++

