#!/bin/bash
set -x

if [[ "$LFS_ENV" != "m1_root" ]];then
 echo $(basename "$0") "must be run from env_m1_root"
 exit 1
fi

m2fs_unbind
umount -v "$LFS_M2_FS/boot" "$LFS_M2_FS" "$LFS_M2_EFI"

rmdir -v "$LFS_M2_FS"/mnt && rmdir -v "$LFS_M2_FS" 
rmdir -v "$LFS_M2_EFI"

