#!/bin/bash

set -x
pushd "$SOURCES"/gcc-11.2.0

# mv -v gmp-6.2.1 gmp
# mv -v mpfr-4.1.0 mpfr
# mv -v mpc-1.2.1 mpc

case $(uname -m) in
  x86_64)
    sed -e '/m64=/s/lib64/lib/' \
        -i.orig gcc/config/i386/t-linux64
 ;;
esac

popd
set +x
