#!/bin/bash
source package_untar "make"
./configure --prefix=/usr   \
            --without-guile \
            --host=$M2_ROOT_TGT \
            --build=$(build-aux/config.guess)
make
make DESTDIR=$M2_ROOT install
cd $M2_ROOT
rm -rf source/$SOURCE_DIR
