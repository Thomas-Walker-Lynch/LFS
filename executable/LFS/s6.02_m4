#!/bin/bash
echo \#--------------------------------------------------------------------------------
echo \# $0

source package_untar "m4"
./configure --prefix=/usr   \
            --host=$M2_ROOT_TGT \
            --build=$(build-aux/config.guess)
make
make DESTDIR=$M2_ROOT install

cd $M2_ROOT
rm -rf source/$SOURCE_DIR
