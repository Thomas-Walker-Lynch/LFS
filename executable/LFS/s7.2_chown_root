#!/bin/bash
echo \#--------------------------------------------------------------------------------
echo \# $0
#!bin/bash

chown -R root:root $M2_ROOT/{usr,lib,var,etc,bin,sbin,tools}
case $(uname -m) in
  x86_64) chown -R root:root $M2_ROOT/lib64 ;;
esac