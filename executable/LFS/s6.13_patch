#!/bin/bash
source package_untar "patch"
./configure --prefix=/usr   \
            --host=$LFS_TGT \
            --build=$(build-aux/config.guess)
make
make DESTDIR=$LFS install
cd $LFS
rm -rf source/$SOURCE_DIR
